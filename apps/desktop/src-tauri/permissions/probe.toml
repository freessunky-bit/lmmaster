"$schema" = "../gen/schemas/desktop-schema.json"

# Phase 1A.4.b: 환경 점검 명령 권한 — main 윈도가 detect_environment 호출 가능.

[[permission]]
identifier = "allow-detect-environment"
description = "Run detect_environment to gather hardware/runtime information for the wizard."
commands.allow = ["detect_environment"]
